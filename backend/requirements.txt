# CollectIQ backend — portable runtime dependencies (public PyPI only).
# Pinned to the versions verified working during setup.
# The original full pip-freeze (incl. Emergent-only packages) is kept in
# requirements.full.txt for reference.

# --- Web framework / server ---
fastapi==0.138.1
uvicorn[standard]==0.49.0
starlette==1.3.1
python-multipart==0.0.32

# --- Database (MongoDB, async) ---
motor==3.7.1
pymongo==4.17.0

# --- Auth ---
PyJWT==2.13.0
bcrypt==5.0.0
passlib==1.7.4

# --- Models / validation / config ---
pydantic==2.13.4
email-validator==2.3.0
python-dotenv==1.2.2

# --- AI extraction (PDF/Excel -> structured JSON via Google Gemini) ---
google-genai==2.10.0
pandas==2.2.3
openpyxl==3.1.5

# --- Report export (PDF + Excel) and Notion sync ---
fpdf2==2.8.2
httpx==0.28.1
certifi==2025.1.31
